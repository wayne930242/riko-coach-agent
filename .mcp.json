{
  "mcpServers": {
    "health-coach": {
      "command": "uv",
      "args": ["run", "--project", "${CLAUDE_PLUGIN_ROOT}/mcp-server", "python", "${CLAUDE_PLUGIN_ROOT}/mcp-server/server.py"],
      "env": {}
    }
  }
}
